# Normalise line endings: LF in the repo for everything text.
* text=auto eol=lf

# Windows-only helpers keep CRLF.
*.cmd text eol=crlf
*.bat text eol=crlf

# Binary assets (none yet, but be explicit).
*.png binary
*.gif binary
*.zst binary
